#pragma once

struct DeliveryBlock {
    int row;
    int col;
    int height;
    int width;
};

struct PickupBlock {
    int row;
    int col;
    int height;
    int width;
};

struct IdleBlock {
    int row;
    int col;
    int height;
    int width;
};